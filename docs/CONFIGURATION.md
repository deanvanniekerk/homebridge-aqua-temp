# Configuration checkpoint

The UI schema and runtime parser validate credentials, optional device IDs, the 30–300 second poll interval and debug settings before network traffic. Schema/runtime parity is tested. Diagnostics helpers are present; live transition reporting and report access are delivered in the following diagnostics PR for #8.
