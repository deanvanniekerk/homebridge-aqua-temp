# Local validation report

Recorded 2026-09-08 for the limited Heat integration in issues #5, #7 and #9. These results establish local software behavior within the documented support boundary, not physical actuation or production stability. No real account, device-setting command, production Homebridge storage or hardware was used by the automated tests.

## Reproduce

Use Node 22.23.2 and npm 10.9.8, then run:

```sh
npm ci --no-audit --no-fund
npm run check
bash scripts/check-armv7.sh
```

The ARM command requires a clean committed checkout. It runs the same checks in a pinned Linux ARMv7 container using emulation; see [development instructions](DEVELOPMENT.md). The native development host is macOS arm64. Homebridge is pinned to 2.4.0 (its supplied HAP is 2.2.2). The plugin has no runtime dependencies. Installed consumer dependencies are resolved from the pinned Homebridge package as described in DEVELOPMENT.md.

## Checked results

The earlier read-only checkpoint passed 75 tests, with minimum/latest Node 22 on Linux x64 and pinned emulated Linux ARMv7 CI in merged PRs #15–#19. This delivery extends the actual gateway, packed Homebridge process and virtual soak to supported commands. Final verification for this delivery is recorded in its PR checks; commands above reproduce the suite. Tests never load the real account or private evidence.

At completion snapshot `afe5a69`, `npm run check` passed formatting, lint, typechecking and all 78 tests on native macOS arm64 / Node 22.23.2. The test phase took about 44 seconds. The standalone model and adapter snapshots each passed their 77-test suites. Both review passes are clear after correcting an intermediate package assertion and adding the explicit target-reconciliation regression. Each completion PR also runs minimum/latest Node 22 x64 and emulated ARMv7 CI; inspect its checks for the final merge result.

## Evidence by boundary

| Requirement                                                                                   | Evidence                                                                                                                                             | Scope                                                                                                                                                                                                                      |
| --------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Authentication concurrency, expiry, contention and denied access                              | [Cloud transport tests](../test/cloud.test.mjs)                                                                                                      | Real HTTP with shared login, one renewal/read replay, cooldown and account/device permission distinctions.                                                                                                                 |
| Malformed JSON/envelopes, missing tokens, oversized/incomplete bodies, HTTP-200 vendor errors | [Cloud transport tests](../test/cloud.test.mjs)                                                                                                      | Real HTTP parsing, bounded bodies, safe closed error categories.                                                                                                                                                           |
| DNS, TLS and connection failures; timeouts, redirects, 429/5xx and Retry-After                | [Cloud transport tests](../test/cloud.test.mjs)                                                                                                      | DNS failure injected at the resolver boundary; real HTTPS/HTTP request code remains active. TLS negotiation/connection refusal tested on local servers. No real vendor DNS or network outage is induced.                   |
| Partial/shared/duplicate discovery and normalized telemetry                                   | [Gateway tests](../test/gateway.test.mjs), [device-model tests](../test/device-model.test.mjs)                                                       | Actual client/gateway requests, sanitized observations and labeled synthetic cases.                                                                                                                                        |
| Poll scheduling, staleness, isolation, cancellation and bounded subscriptions                 | [Coordinator tests](../test/coordinator.test.mjs)                                                                                                    | Actual coordinator with deterministic scheduler; separate real-gateway shutdown test closes outstanding login work.                                                                                                        |
| Queue deadlines, ambiguous results, no replay and late-result reconciliation                  | [Command tests](../test/commands.test.mjs), cloud transport tests                                                                                    | Focused queue/transport tests isolate timing and retries; the soak additionally runs accepted and lost-acknowledgment writes through the actual profile, gateway and HAP. No test proves physical actuation.               |
| HAP values, constraints, errors and setter deadlines                                          | [Thermostat tests](../test/thermostat.test.mjs)                                                                                                      | Actual HAP handlers check constraint intersections and deadlines; the packed host verifies the supported real gateway and command/readback path.                                                                           |
| Config rejection, initial login denial, normal/debug redaction and reports                    | [Configuration tests](../test/configuration.test.mjs), [diagnostic tests](../test/diagnostics.test.mjs), [platform tests](../test/platform.test.mjs) | Runtime/schema agreement, bounded messages, actual transport/platform failures and explicit field projection.                                                                                                              |
| Tarball install, cached identity, cold/warm restart and child bridges                         | [Package tests](../test/package.test.mjs)                                                                                                            | Clean production-only Homebridge install, actual HAP HTTP reads, confirmed target/Off/Heat writes and accepted-but-unconfirmed errors. Test-host-only routing supplies a fake cloud; it is excluded from the distribution. |
| Seven-day repeated fault/recovery schedule                                                    | [Read/write recovery simulation](../test/recovery-soak.test.mjs)                                                                                     | Actual HTTP client, gateway, coordinator, diagnostics and HAP; two synthetic devices and virtual scheduling.                                                                                                               |

## Seven virtual days

The simulation runs at the supported 300-second poll interval for 604,800,000 milliseconds of virtual time, including the initial poll: 2,017 polls. It repeatedly alternates 503 and 429 outages with five-minute Retry-After, malformed shared discovery, empty/duplicate lists, per-device 403, offline state, malformed JSON/telemetry and external temperature changes. The session expires at each virtual day boundary. It checks staleness after three intervals, recovery without restarting, per-device isolation and retained identities.

The extended native simulation observed 11,704 HTTP requests, eight logins (initial plus seven renewals), 84 explicit target-setting requests, 5,870 notifications and 3,629 sanitized log/report lines. Forty-two writes succeed with R02 readback; forty-two apply at the fake device but lose the response connection, producing a HAP error and later read-only reconciliation. Set_Temp is not used to confirm the target. Request counts prove that recovery never repeats the lost writes. A separate [target recovery regression](../test/control-recovery.test.mjs) keeps the applied 31.5°C state through backoff, asserts that the actual HAP target adopts it, then changes the fake app target to 30.5°C and asserts a later poll adopts that value with the write count still one. The seven-day soak replaces app values at cycle boundaries; it does not alone prove the intermediate applied-target observation.

Maximum simultaneous server sockets was two; active timer resources peaked at five above baseline. Assertions bound request/notification/log counts, retained scheduler tasks, update subscriptions and shutdown cleanup. Counters are retained instead of all requests/logs. Real HTTP settles at each virtual scheduler event before time advances again; this prevents the test harness from falsely timing out OS I/O. Virtual duration remains exactly seven days. Native wall-clock execution was about fourteen seconds in the focused run; this is not an endurance claim.

The production-only packed host performs five lifecycles (normal cold/warm/recovery and child-bridge cold/warm). Each cycle reads water/target, writes 32.5°C, turns On and Off, restores 32°C, then injects an acknowledged-but-ignored target request. The latter returns a HAP communication error and preserves reported 32°C. It asserts exactly five explicit writes per lifecycle and no startup, restart or shutdown writes. The observed HAP target range is 15–38°C / 0.5°C, and identity persists across same-bridge restarts.

The initial ARMv7 control runs exposed a startup race in the package test: HAP snapshots characteristic metadata before awaiting getters, so discovery can contain fresh temperature values alongside earlier read-only metadata. The harness now waits for writable target permissions before exercising controls, then independently asserts the exact bounds and step. No timeout, retry budget or production behavior was changed to address this failure.

## Clock-correction regression

A real HTTP regression test moves `Date.now()` back one day while leaving the process monotonic clock intact. Before the fix, the newly acquired sample was immediately classified as stale. The gateway and diagnostics now use the same default monotonic epoch clock as the coordinator. The test verifies fresh state and a one-minute diagnostic age under the clock correction. This fixes mixed-clock freshness accounting; it does not infer vendor measurement timestamps.

## Limits and remaining gates

The [supported profile and limitations](DEVICE_MODEL.md#supported-control-subset-and-limitations) are explicit. No unverified Mode writes are enabled. The owner-assisted 32 → 32.5 → 32°C test through the installed iHost plugin below now establishes one physical-account fractional target round trip; it does not validate every grid point. Active heating versus defrost cannot be distinguished; unknown activity returns a HAP error. The local control path is implemented and exercised, with this intentionally limited activity representation.

Virtual scheduling is not seven days of real-world uptime or a memory-leak proof. The simulation keeps two identities stable; it does not establish unlimited account/device scaling. Real-account session behavior, write authority and physical actuation remain separate from synthetic test outcomes.

Homebridge's child-bridge SIGTERM fallback may produce host exit 143; this is distinguished from ordinary host exit 0 and plugin crashes. HAP's `/accessories` serializer can substitute default values after getter failure, while `/characteristics` returns communication errors. See [adapter limitations](HOMEBRIDGE_ADAPTER.md). Neither accepted host fallback nor local HAP responses prove actual Apple Home presentation or physical iHost shutdown behavior.

The CI workflow declares minimum/latest Node 22 on Linux x64 and pinned emulated ARMv7. Inspect the delivery PR checks for their exact commit and results; local results alone do not establish CI success. Docker Desktop execution does not prove mDNS discovery, Apple Home pairing, physical iHost compatibility, or the hardware soak in issue #10.

## Physical iHost validation — issue #10

Preparation started 2026-09-09 against merged delivery `5e7c51f`. The development tarball SHA-1 reported by npm pack is `7c680116c5ee66296485f51e3d7ba339bf04868c`; this identifies the build under evaluation, not a signature or release certification. Host inventory, runtime upgrade and tarball installation are recorded below; cloud-backed startup and direct HAP reads have succeeded; Apple Home validation is still pending. Earlier owner-assisted protocol observations remain in DEVICE_MODEL.md and do not substitute for testing the installed plugin through Apple Home.

| Stage                                   | Evidence required                                                                                                                                                   | Current status                                                                                                                                                                                                                                                                                                  |
| --------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Host inventory and recovery preparation | Current Node/Homebridge/UI versions, architecture, storage/network mode, old-plugin status, local backup and rollback route before any upgrade                      | Upgraded 2026-09-09 to Homebridge 2.4.0 / Node 22.23.2 on ARMv7. UI remains 5.11.0. Fresh backup inspected; full restore not tested.                                                                                                                                                                            |
| Read-only account check                 | Dedicated shared-account discovery, online status and normalized values compared with the app; no competing integration session                                     | Both old Aqua Temp packages disabled, with accessory retention enabled. Owner entered the shared-account credentials locally. Discovery succeeded for one supported device. Owner confirmed the post-outage app readings of 20°C water and 32°C target.                                                         |
| Installation and child-bridge pairing   | Supported host, exact artifact, configuration, separate child bridge and owner pairing in Apple Home                                                                | Reviewed tarball installed and checksum verified. Configured Aqua Temp Test as a separate child bridge and verified startup. Owner paired successfully; Apple Home showed 18°C and No Response. See decoder diagnosis below.                                                                                    |
| Readings and errors                     | App versus Home water/target, Off/Heat and units; actual Home presentation when activity is unknown                                                                 | Pairing and No Response error presentation observed. Corrected build recovered to Off in Apple Home on macOS. Post-outage HAP/Home water was 20°C and HAP target was 32°C; the owner independently confirmed both in Aqua Temp. Unknown activity is not verified heating.                                       |
| Coordinated controls                    | Record original power/mode/target; agree a reversible target test (including a half-degree value), observe app outcome and restore; separately agree any power test | Target-only 32 → 32.5 → 32°C passed through installed HAP handlers, with independent owner app confirmation in both directions and power remaining Off. A coordinated On → Off installed-HAP round trip also succeeded and Apple Home followed both states; independent owner app confirmation remains pending. |
| App changes and restart                 | App-originated target reflected after polling; child-bridge restart retains identity and sends no unsolicited commands                                              | Pairing survived diagnostic and corrected-build child restarts; no re-pair needed. Owner app target 31.5°C reached HAP through ordinary polling. Intentional test preparation/cleanup restarts also retained pairing and restored readings; no re-pair/reset was required.                                      |
| Connectivity recovery                   | Owner-agreed outage isolated to the test integration, stale/error presentation and automatic recovery; preserve unrelated iHost services                            | Passed a child-process DNS failure injection on 2026-09-09: stale HAP errors and Home No Response, then same-process automatic recovery. Physical WAN/router failure remains untested.                                                                                                                          |
| Seven-day real-time run                 | UTC start/end, artifact/runtime versions, poll interval, restart counts, resource observations, requests where observable, incidents and manual interventions       | Not started. Virtual tests are not elapsed hardware uptime.                                                                                                                                                                                                                                                     |

Record each completed stage with time, observation source, result and limitations. Keep account/device identifiers, credentials, pairing codes, backup contents and raw private logs out of this report. Leave unavailable criteria open and the package labeled development/prerelease. No scheduled monitoring or background execution has been configured during preparation.

### Host inspection, 2026-09-09

The signed-in Homebridge dashboard reports Docker, Ubuntu Noble 24.04.1, 32-bit ARM and storage `/var/lib/homebridge`. A targeted terminal read confirms Node 22.13.1 / ARM version 7, local Homebridge 1.11.1 and global UI 5.11.0. The dashboard shows the Aqua Temp, Midea and Sunsynk child bridges running. Installed Aqua Temp packages are `@rogchap/homebridge-aqua-temp` 2.9.1 and `@deanvniekerk/homebridge-aqua-temp` 1.0.2; neither is the new `@deanvanniekerk` development package. Which old package supplies the running bridge has not yet been checked.

The Backup & Restore dialog shows automated backups enabled and dated stored archives, plus download and restore controls. Archive contents, non-registry plugin recoverability and a restore have not been verified. No backup, configuration, package or runtime change was made during this inspection. A separate test container with independent storage is being investigated before proposing an upgrade to the shared production runtime. iHost administration requires owner sign-in; the login page reports version 2.13.2.

### Existing-container upgrade and installation, 2026-09-09

The owner chose the existing Homebridge Docker container for testing after discussion of the shared runtime. Adding the official image through iHost's Add to list flow retained the existing listing; no separate container was created. The existing container remains in host network mode with `/homebridge` storage. Midea 1.3.4, Sunsynk 1.1.5 and both installed legacy Aqua Temp versions declare engine ranges that include Homebridge 2.4.0 / Node 22.23.2 (registry metadata checked); that is a compatibility declaration, not physical-device certification.

A fresh backup was generated in Homebridge's configured backup directory. A read-only tar inspection verified a nonempty 19,035-byte archive with 33 members, including configuration and pairing persistence. No package.json member was present. The download action was also invoked, but an off-host downloaded copy was not located or verified. The prior package.json and package-lock.json, where present, were separately preserved with private permissions under `/var/lib/homebridge/backups/issue-10-runtime`. Restore and downgrade have not been exercised; the backup is not claimed to include the old runtime binaries or installed plugin code.

Using the supported `hb-service update-node 22.23.2` command installed the selected Node patch under `/opt/homebridge`. Installing exact `homebridge@2.4.0` in the existing storage directory completed successfully. The UI was not upgraded: 5.11.0 remained available for configuration and restart. See the official [service command](https://github.com/homebridge/homebridge-config-ui-x/wiki/Homebridge-Service-Command) and [Homebridge 2 migration guidance](https://github.com/homebridge/homebridge/wiki/Updating-To-Homebridge-v2.0).

The running legacy platform was `DvnAquaTemp`, supplied by `@deanvniekerk/homebridge-aqua-temp` 1.0.2; `@rogchap/homebridge-aqua-temp` 2.9.1 was already disabled. The initial Disable dialog warned of lost HomeKit data with accessory retention off, so it was cancelled. **Keep Accessories Of Uninstalled Plugins** was then enabled and saved before disabling the legacy plugin. The subsequent dialog stated that old accessories would remain in HomeKit as No Response and retain their scenes/automations; its displayed boolean label was inconsistent with the checked setting. A Homebridge restart applied the runtime and configuration changes. The dashboard then showed the main bridge paired and Midea/Sunsynk child bridges Running, with the old Aqua Temp bridge absent. No unrelated accessory was actuated to verify those status indicators.

The reviewed development tarball was transferred from the development Mac over a temporary LAN HTTP endpoint restricted to the iHost source address and one artifact path. The endpoint served no repository directory or private files and was stopped immediately after transfer. The iHost verified SHA-1 `7c680116c5ee66296485f51e3d7ba339bf04868c` before installing `/var/lib/homebridge/aqua-temp-development.tgz`; npm reported one package added. This is a local tarball installation, not a registry release. Keep the tarball for reinstall/rollback because ordinary UI backup/restore does not promise restoration of non-registry plugins.

At 05:29 UTC, the new `@deanvanniekerk/homebridge-aqua-temp` 0.0.0-development.0 appears in the UI with its setup form open. Prepared values are name **Aqua Temp Test**, 60-second polling, no device-ID restriction and sanitized debug diagnostics enabled. Credentials have not been entered or saved, no new-plugin cloud session has started, and no heat-pump command has been sent. The next owner-assisted step is local shared-account credential entry, followed by isolated child-bridge configuration, read-only startup and Apple Home pairing.

### Cloud-backed startup and HAP reads, 2026-09-09

The owner entered the dedicated shared-account credentials locally and saved the configuration. The separate child bridge was named **Aqua Temp Test**, saved and restarted. At 05:32 UTC, logs reported Homebridge 2.4.0 / HAP 2.2.2 and Node 22.23.2. Sanitized diagnostics confirmed completed discovery, one supported boost-i-hp40 device, healthy status and available controls, with no account or device failure. The dashboard showed the main bridge paired and the new child bridge, Midea and Sunsynk running. These indicators do not establish physical control of any accessory.

Read-only requests to the installed child's HAP `/characteristics` endpoint returned water **18°C**, target **32°C**, target state **Off** and Celsius display units, each with status 0. Current activity returned communication error **-70402**. The test used the host's already-enabled insecure HAP access; no security setting was changed. `/characteristics` was used because `/accessories` may substitute defaults after getter failure. Actual Apple Home presentation and comparison with the Aqua Temp app remain pending. No heat-pump setting command was sent.

Startup also emitted a Target Temperature characteristic warning: the initial value 10 was below the newly applied minimum 15. The subsequent target read was correctly 32°C. This warning requires investigation and a regression fix before completing validation; it is not evidence that the heater target was changed to 10°C. The child-bridge pairing screen is open for the owner to add **Aqua Temp Test** in Apple Home.

### Apple Home failure and diagnosis, 2026-09-09

Owner screenshots at 07:43–07:44 local (05:43–05:44 UTC) establish successful pairing and an **18°C / No Response** tile. Accessory details showed Celsius and the expected model, but Apple Home withheld its normal controls and reported that it could not communicate with the bridged accessory. A direct HAP check reproduced a communication failure only for CurrentHeatingCoolingState; the other four required thermostat readings succeeded. This confirms that per-characteristic activity failure is not a usable partial thermostat presentation in this observed Apple Home setup.

A temporary one-shot diagnostic was added to the installed gateway, with the original file preserved privately. Only boolean fault flags and a bounded projection of the O07 datatype/value shape were logged; no credentials, identifiers or additional cloud requests were introduced. Three agent-initiated diagnostic child-bridge restarts narrowed the datatype projection. Both fault aliases were boolean false; the single O07 field contained string value `0` and datatype `DIGI1`. The earlier status-parameter fixture used a projection where null meant metadata omitted by the projection, so it did not establish that the real O07 datatype was absent. The normalizer's null-only datatype condition rejected this valid inactive reading.

The decoder now accepts the observed `DIGI1` metadata as well as its prior nullish form, only for exactly parsed zero frequency with the existing Heat/power/clear-fault checks. Positive, duplicate, malformed and unsupported metadata still fail closed. The existing model regression was extended with the observed datatype and failed before this correction. A separate real-HAP regression reproduced the fresh-discovery target warning: applying a 15°C minimum to HAP's default 10°C could publish a clamped 15°C before the confirmed 32°C. Invalidating the old target before updating constraints prevents this fabricated intermediate notification and warning; the regression then passes. Neither fix issues device commands.

The diagnostic restarts were manual interventions, not a soak or plugin crashes. Homebridge's child termination fallback returned 143, then the child restored one cached accessory. Updated artifact installation and owner confirmation of Apple Home recovery are pending. Unknown running activity remains a limitation and must not be relabeled as verified heating merely to clear the tile.

### Corrected installation and Apple Home recovery, 2026-09-09

`npm run check` passed formatting, lint, typechecking and all **79 tests** on macOS arm64 / Node 22.23.2; test execution took about 27 seconds. Standards review found no actionable issues. Specification review requested correction of stale pending-status wording, which was updated; the remaining physical criteria stay open. The modified decoder regression and new real-HAP startup regression both failed before their respective fixes. Neither headless Claude review nor a new CI/ARM-emulation run was performed for this checkpoint.

The corrected development artifact has SHA-1 `a7f219c1b3ec4dc521ba8e20e77ab096b7abb1bf`, 21,513 bytes and 20 package entries. The version remains 0.0.0-development.0; use the checksum to distinguish this build. A temporary source-IP/path-restricted LAN endpoint transferred only this tarball and was stopped after transfer. The iHost verified the checksum, then npm installed `/var/lib/homebridge/aqua-temp-issue10.tgz` (one package changed). Both fixes were checked in the installed files. The original gateway was restored before installation, and no temporary diagnostic remains in the new package.

Only Aqua Temp Test was restarted to load the corrected build. At 05:55:28 UTC the child restored one cached accessory, and at 05:55:32 diagnostics showed healthy supported-device discovery. The corrected startup log showed no target-default warning. Direct HAP reads succeeded for all five required thermostat characteristics: current activity idle (0), target state Off (0), water 18°C, target 32°C and Celsius display units (0).

The owner authorized inspection of Apple Home on the Mac. Its tile initially retained No Response after the child restart. By the 05:59 UTC observation, opening the accessory details and returning to the Home view showed **Aqua Temp Test, Off** without the communication-error banner; More Controls then showed **Current 18.0°** and the normal Off mode control. Pairing was retained and no re-pair/reset was performed. No power, target, mode or display-unit control was changed during inspection. This establishes recovery for the observed inactive baseline, not a usable representation of unknown running activity. Independent Aqua Temp app comparison, coordinated writes, app-originated changes, planned outage recovery and the seven-day real-time run remain pending.

### Coordinated fractional target test, 2026-09-09

The owner agreed to watch Aqua Temp during a target-only 32 → 32.5 → 32°C test with requested power remaining Off. Before the first write, HAP reported Off and 32°C with successful statuses. At 06:01:47.968 UTC, a single HAP TargetTemperature write of 32.5°C returned HTTP 204 through the installed plugin; immediate HAP reads then reported 32.5°C and Off, both successful. No power/mode write was issued. The owner independently confirmed that Aqua Temp displayed 32.5°C. At 06:03:04.979 UTC, a single target-only restoration write of 32°C returned HTTP 204; immediate HAP reads reported 32°C and Off with successful statuses. The authorized round trip therefore has successful installed-plugin readback in both directions and an independent app observation of the fractional value. The owner also independently confirmed that Aqua Temp returned to 32°C, completing the agreed round trip. These observations establish reported settings, not physical heat production; no power/mode command was issued.

### App-originated target change, 2026-09-09

The owner set the target to 31.5°C in Aqua Temp with power remaining Off and reported completion. A read-only HAP sample at 06:07:41.734 UTC still showed 32°C. The next bounded observation at 06:08:28.660 UTC showed 31.5°C and Off, both status 0. Only HAP GET requests were issued during observation; they do not initiate cloud reads. The installed coordinator therefore adopted the app-originated value through its ordinary 60-second polling cycle. The user's exact change time is not known, so these timestamps do not establish a precise end-to-end latency guarantee.

After confirming 31.5°C and Off again, one target-only restoration request through HAP returned HTTP 204 at 06:09:07.697 UTC. Immediate HAP readback showed 32°C and Off, both status 0. No power or mode command was issued. The owner independently confirmed the second restoration to 32°C in Aqua Temp. Apple Home does not display the target while the thermostat is Off; this observation establishes the target exposed through HAP, not a visible Apple Home target-slider update.

### Isolated resolver outage, 2026-09-09

A temporary development-only DNS injection helper was prepared under `.local/issue-10-outage/`, outside the distributable package. The initial plan was to load it through Aqua Temp Test's child-bridge NODE_OPTIONS; it does not change the system resolver, firewall, vendor endpoint, credentials or other plugins. A local flag enables failure for the exact `cloud.linked-go.com` hostname. Removing the flag releases the failure on the next lookup; a monotonic five-minute maximum also releases it even if the flag is left behind. After release, the same process cannot be re-armed accidentally.

A local Node 22.23.2 check exercised healthy DNS resolution, the exact-host ENOTFOUND failure through both DNS and a real HTTPS request, unrelated localhost resolution, release by flag removal, release by a shortened automatic deadline, and resistance to re-arming. Both scenarios passed. No login or heater-setting request was sent by these checks. These local checks established the helper behavior before the host experiment; neither the local checks nor the host DNS injection establish physical WAN-disconnection behavior.

The planned sequence was: preserve the child's existing environment settings, load the helper and restart only that child, establish fresh healthy readings, enable the flag, observe staleness/Apple Home No Response, remove the flag, verify cloud/HAP/Home recovery without a restart during recovery, then remove the helper option and perform a cleanup restart. Record every intentional restart separately from unplanned failures. Real elapsed recovery includes the plugin's bounded retry delay. The heater remains Off at 32°C and no setting commands are part of this experiment. Other installed plugins and the home network remain outside the fault injection.

The owner authorized the controlled test. The first preparatory child restart retained healthy HAP readings but did not load NODE_OPTIONS: inspection of installed Homebridge 2.4.0 showed that child environment construction happens at initial bridge startup, while child restart refreshes the plugin configuration without rebuilding the environment. The original environment configuration was restored. To avoid restarting other bridges, the helper was instead imported temporarily from only this plugin's installed entry file, with a byte-for-byte private backup. The distributable tarball was unchanged. After a second preparatory child restart, HAP at 06:25:03.980 UTC showed activity Idle, requested power Off, water 20°C, target 32°C and Celsius units, all status 0; the child listening on port 32506 was PID 2665. Apple Home's open controls also showed Current 20.0° and Off. The fault flag was enabled at 06:25:17.844 UTC, and the helper subsequently logged that it was blocking cloud DNS in this process. No device-setting command was sent.

The injected failure was caught: the 06:26:05 UTC diagnostics reported discovery failure and a 23,275 ms retry delay while retaining the recent device reading. At 06:27:55 UTC, the device became stale with last-success age 180,002 ms. The first subsequent HAP observation at 06:28:06.253 UTC returned -70402 for current activity, requested power, water temperature and target temperature; static display units remained successful. At 06:28:13.256 UTC the listener was still PID 2665. Apple Home initially retained the already-open healthy controls; dismissing that sheet exposed the No Response state, and the Home overview then explicitly showed Aqua Temp Test, No Response. This is observed error presentation after refreshing the view, not proof that every open Apple Home screen updates immediately. The failure flag was removed at 06:28:54.334 UTC without restarting the child.

The helper released on the next DNS lookup and reported seven blocked lookups. At 06:33:33 UTC, diagnostics reported account discovery and fresh device readings recovered. The next HAP sample, 06:33:36.966 UTC, returned status 0 for all five values: Idle, Off, water 20°C, target 32°C and Celsius units. At 06:33:50.551 UTC the listener remained PID 2665, proving recovery without process replacement. Approximately 4 minutes 39 seconds elapsed between flag removal and the recovery log; this includes the existing bounded retry delay and is not a universal latency guarantee. Apple Home initially retained its No Response tile after HAP recovery. Opening the accessory details and returning to the overview showed **Aqua Temp Test, Off** by the subsequent 06:34 UTC observation, without restarting, re-pairing or resetting it. No target, power or mode command was issued during the experiment.

A resource snapshot during the outage showed the Aqua Temp child at 89,936 KiB RSS; one snapshot is not a memory-leak or long-term stability assessment. The main bridge and Sunsynk listener processes started at 05:31:43.970 and 05:32:08.350 UTC, before test preparation, and remained running when inspected. No system DNS, firewall, router or unrelated plugin configuration was changed. This establishes process continuity for the observed services, not functional actuation tests of unrelated accessories.

Cleanup restored the plugin entry byte-for-byte (SHA-256 `3ac36cff2ab3b83c1e25a9061fc3714daaed259c234abbdd4162e5c6ea6333a4`) and verified that parsed Homebridge configuration exactly matched the private pre-test backup. The helper and its flag were deleted before a separate intentional cleanup child restart. The two preparatory restarts and cleanup restart are manual test interventions and are excluded from any future unattended-soak claim. The DNS-injection gate is complete; the seven-day real-time run has not started.

At 06:35:37.605 UTC, after the intentional cleanup restart, the unmodified plugin listener was PID 2880 with no test preload in its environment. All five HAP reads again succeeded with Idle, Off, 20°C water, 32°C target and Celsius units. The Apple Home overview retained the recovered Off tile. The bounded HAP observation process and temporary LAN transfer server both ended; no recurring monitor was configured.

### Owner app comparison after outage recovery

On 2026-09-09, the owner explicitly confirmed that Aqua Temp displayed **20°C water and 32°C target**, matching the post-cleanup HAP sample and Apple Home current-temperature reading. This completes the independent numeric comparison for the observed inactive baseline. It does not establish a visible target in Apple Home while Off, active heating/defrost identification, or a live installed-plugin power-control round trip.

### Coordinated installed-plugin power test, 2026-09-09

The owner authorized an On → Off test while watching Aqua Temp, keeping the target at 32°C. Before dispatch, successful HAP reads confirmed requested power Off and target 32°C. At 06:39:36.940 UTC, one TargetHeatingCoolingState=Heat request through the installed HAP handler returned HTTP 204; immediate readback confirmed On and 32°C with status 0. This handler controls power only while the verified vendor mode is already Heat; no vendor-mode or target write was sent. Apple Home subsequently changed from Off to **Heat to 32.0°**. Its open controls showed **Current 20.0°**, target **32.0°** and **Heat**, without a No Response banner at that observation. At 06:40:14.098 UTC HAP reported Idle, requested Heat, water 20°C, target 32°C and Celsius units, all successful. The requested Heat setting must not be interpreted as evidence of active compressor heating. Independent owner app confirmation remains pending; the Off restoration is recorded below.

Bounded HAP observations from 06:40:14.098 through 06:42:14.354 UTC continued to report requested Heat with Idle activity and no HAP errors; they did not establish compressor operation. At 06:42:30.599 UTC, after successful preflight reads reconfirmed On and 32°C, one TargetHeatingCoolingState=Off restoration request returned HTTP 204. Immediate HAP reads confirmed Off and target 32°C with status 0. Apple Home’s open controls subsequently returned to Off. The experiment sent exactly two power requests and no target or vendor-mode requests, with about 2 minutes 54 seconds between acknowledgments. No plugin restart was needed. This verifies installed-plugin power-command confirmation and Apple Home requested-state presentation for the observed idle interval; running activity, defrost distinction and independent app confirmation of this specific power round trip remain unverified.
